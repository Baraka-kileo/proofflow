# Implementation evidence log

This is an append-only record of proof for completed checklist units. Do not erase failed checks; add the fix and rerun result beneath them. Never include credentials, tokens, personal data, or confidential document contents.

## Entry template

```text
Date/time:
Agent/person:
Work unit IDs:
Outcome:
Changed files:
Automated checks and exact result:
Manual/browser checks and viewport:
Security/tenant checks:
Assumptions or demo-only behavior:
Remaining risks/blockers:
Commit/CI link:
```

## 2026-09-05 — P0 foundation

- **Work unit IDs:** P0-01, P0-02, P0-03, P0-04
- **Outcome:** Created private GitHub repository, product documentation, agent delivery rules, Next.js 16 foundation, initial ProofFlow landing experience, and CI.
- **Changed areas:** root configuration, `src/app`, `docs`, `.github/workflows/ci.yml`.
- **Automated evidence:** `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` passed locally. GitHub CI run `33924461681` passed clean install, lint, typecheck, and build on commit `a13de82`.
- **Manual evidence:** Landing page inspected at desktop and 375px-wide mobile. Mobile `scrollWidth` equalled `clientWidth`; no console warnings/errors were observed. Motion settled correctly and reduced-motion CSS is present.
- **Security/demo boundary:** Repository is private. `.env.local`/`.env*` are ignored except the empty `.env.example`. Landing copy states no real money moves.
- **Remaining:** P1 and later application functionality is not implemented. The landing page alone is not evidence that the role portals work.

## 2026-09-05 — P1-01 approved dependencies

- **Work unit IDs:** P1-01
- **Outcome:** Installed the approved Supabase, Gemini, Zod, Lucide, Radix/shadcn utility, toast, Vitest, Testing Library, and Playwright dependencies without adding a second form, state, or UI system.
- **Changed files:** `package.json`, `package-lock.json`.
- **Automated evidence:** `npm audit --omit=dev` reported zero vulnerabilities. `npm run lint`, `npm run typecheck`, and `npm run build` passed with Next.js 16.3.4 and Node 24.
- **Assumptions:** Packages are installed but external Supabase/Gemini services are not configured or claimed working.
- **Remaining:** P1-02 must replace the temporary `test` alias with genuine unit and browser smoke test commands.

## 2026-09-05 — P1-02 real test harness

- **Work unit IDs:** P1-02
- **Outcome:** Configured genuine Vitest/jsdom unit testing and Playwright Chromium browser testing; CI now executes unit tests.
- **Changed files:** `package.json`, `package-lock.json`, `vitest.config.mts`, `playwright.config.ts`, `tests/setup.ts`, `tests/unit/landing-page.test.tsx`, `tests/e2e/landing.spec.ts`, `.github/workflows/ci.yml`.
- **Automated evidence:** lint and typecheck passed; Vitest ran one semantic landing test; Playwright ran one 390px landing/overflow test in Chromium. Both passed without configuration or server warnings after cleanup.
- **Manual/browser scope:** Playwright asserts the product heading, simulation boundary, and exact mobile scroll width.
- **Remaining:** This is a harness, not coverage of unbuilt role workflows. Feature tests must be added with each later unit.

## 2026-09-05 — P1-03 design tokens and primitives

- **Work unit IDs:** P1-03
- **Outcome:** Added typed ProofFlow Button, Card, StatusBadge, Input, Select, Checkbox, Dialog, Progress, Alert, Skeleton, Toast, Tooltip, FormField, ErrorSummary, and class-merging utilities; expanded the documented color/motion tokens.
- **Changed areas:** `src/components/ui`, `src/components/status-badge.tsx`, `src/components/error-summary.tsx`, `src/lib/utils.ts`, root layout/global CSS, landing page.
- **Automated evidence:** lint, typecheck, production build, and five unit assertions passed. Two Chromium tests passed at 390px, including zero horizontal overflow, visible keyboard focus, and reduced animation duration when reduced motion is requested.
- **Manual/UI boundary:** The landing evidence status now uses the shared StatusBadge. Feature forms will exercise the remaining primitives in P1-04 through P1-07.
- **Remaining:** App shell, route states, login, and role dashboards are not implemented yet.

## 2026-09-05 — P1-04 responsive role shell

- **Work unit IDs:** P1-04
- **Outcome:** Added a typed demo-session DTO, protected route group, desktop rail, tablet/mobile bottom navigation, organization/role context, skip link, top bar, and main content landmark.
- **Changed areas:** `src/app/(protected)`, `src/components/app-shell.tsx`, `src/components/navigation.tsx`, `src/components/logo.tsx`, `src/lib/demo/session.ts`, `src/types/domain.ts`, global CSS.
- **Automated evidence:** lint/typecheck passed; eight unit assertions passed including exact SME/buyer/funder navigation; five Chromium checks passed including shell widths 390, 768, and 1440 with `scrollWidth === clientWidth`.
- **Security/demo boundary:** The adapter is named and labelled demo-only. It is not represented as authentication; P2 must replace it with Supabase/DAL authorization.
- **Remaining:** Unbuilt destinations are exposed as non-interactive `aria-disabled` items rather than dead links. They become links only when their routes are implemented.

## 2026-09-05 — P1-05 global route states

- **Work unit IDs:** P1-05
- **Outcome:** Implemented geometry-stable loading skeletons, executable error reset, not-found recovery, reusable empty state, and online/offline recovery banner in root/protected boundaries.
- **Changed areas:** root/protected `loading.tsx` and `error.tsx`, `not-found.tsx`, route-loading/error, empty-state, connection-status, app shell.
- **Automated evidence:** eleven unit assertions passed, including loading announcement, functional reset callback, and valid empty-state action. Six Chromium tests passed, including forced 404 and offline states with visible recovery.
- **Accessibility evidence:** loading uses a named live status; failures use alert semantics; the offline alert announces assertively; recovery controls are keyboard-native.
- **Remaining:** Feature-specific empty/error states must still be implemented with their feature routes.

## 2026-09-05 — P1-06 honest login and demo roles

- **Work unit IDs:** P1-06
- **Outcome:** Built responsive split login, persistent labelled fields, Zod validation, linked error summary, password visibility control, generic live-auth unavailable notice, and validated demo-role entry.
- **Changed areas:** `/login`, login feature/schema/server action, demo session adapter, login layout CSS, root scroll behavior marker.
- **Automated evidence:** thirteen unit assertions passed, including invalid/valid login behavior and password toggle. Seven Chromium tests passed; buyer demo entry redirected to `/dashboard`, rendered buyer context, and set an HTTP-only cookie.
- **Security/demo boundary:** The role cookie is HTTP-only, same-site lax, secure in production, validated against a fixed enum, and explicitly a demo adapter—not production authorization. No demo passwords are embedded.
- **Remaining:** Supabase authentication/server authorization belongs to P2. Live sign-in clearly says it is not connected.

## 2026-09-05 — P1-07 role dashboards and Phase 1 gate

- **Work unit IDs:** P1-07; Phase 1 gate
- **Outcome:** Built distinct SME, buyer, and funder dashboards over a typed server-only demo repository. SME sees evidence progress; buyer sees age-ordered confirmations; funder sees buyer-confirmed evidence and simulated-offer context.
- **Changed areas:** dashboard page/features, typed demo repository, shared metric/queue presentation, page heading, login/session-aware dashboard test.
- **Automated evidence:** lint, typecheck, unit tests, browser suite, and production build passed. Chromium proved all three role selections land on distinct H1/next-task dashboards. Required 390/768/1440 overflow checks pass.
- **Manual visual evidence:** Inspected login and SME dashboard in the in-app browser. Fixed explicit logo and active-navigation contrast; computed colors are white on dark green. Final console warning/error log was empty.
- **Interaction boundary:** Workflow actions whose routes are scheduled for P3/P6/P7 are real disabled buttons with explanations, not dead links. Nav destinations remain disabled until implemented.
- **Security/demo boundary:** Dashboard data comes from a named server-only demo adapter. No claim of live Supabase isolation is made.
- **Remaining:** P2 requires credentials for live Supabase proof. No P2 unit is checked.

## 2026-09-05 — P2-01 hosted environment boundary

- **Work unit IDs:** P2-01
- **Outcome:** Linked the repository to the healthy hosted ProofFlow Supabase project and added a server-only Zod environment boundary with separate fail-fast checks for live Supabase, privileged Supabase admin, and Gemini extraction workflows. `.env.local` and Supabase CLI link/cache state remain ignored.
- **Changed areas:** `.env.example`, `.gitignore`, `src/lib/env/server.ts`, environment tests, test-only server boundary alias, Supabase CLI project configuration.
- **Failed check retained:** The first unit-test run could not resolve `server-only`. After installing the official boundary package, the second run correctly threw its browser-side guard under jsdom. Vitest now substitutes an explicit no-op only inside tests while production builds retain the real Next.js boundary.
- **Automated evidence:** Final lint and typecheck passed. Six Vitest files with 16 assertions passed, including missing-live-value errors and server-secret guards. The Next.js production build passed. A recursive scan of `.next/static` found neither `SUPABASE_SERVICE_ROLE_KEY` nor `GEMINI_API_KEY` variable names.
- **Security boundary:** No API key, access token, database password, or service-role value was printed, committed, or placed in a public variable. The public Supabase URL/publishable key will be configured separately from server-only secrets.
- **Assumptions:** The current UI may remain in its explicitly labelled demo adapter until P2-07. Live-only helpers fail with actionable configuration errors when invoked without their values.
- **Remaining:** Database schema, storage, RLS, auth helpers, safe seed users, and live dashboard repository remain P2 work.

## 2026-09-05 — P2-02 hosted initial schema

- **Work unit IDs:** P2-02
- **Outcome:** Applied the initial ProofFlow schema to the linked hosted project. It includes every required table and domain enum, UUID ownership chains, timestamps, integer minor-unit money, bounded basis points, and uniqueness for membership, evidence slots, confirmation decisions, funder offers/responses, invoice identity, and document hashes.
- **Changed areas:** `supabase/migrations/20260905020000_initial_schema.sql`, `src/types/database.ts`.
- **Hosted migration evidence:** A dry run listed exactly one pending migration and linked database lint returned no errors. The first hosted push applied migration `20260905020000`; an immediate second push returned `upToDate: true` with zero migrations. Local and remote migration histories match.
- **Type evidence:** Supabase-generated TypeScript database types were saved and compared byte-normalized with a fresh hosted generation; they match exactly. Application lint and typecheck pass, and a final hosted database lint reports no schema errors.
- **Security boundary:** Every new business table had RLS enabled in the initial migration before client access. With no policies yet, browser clients are denied by default. No local Supabase service was started.
- **User-directed deviation:** The checklist's local reset wording was replaced by hosted dry-run, apply, immediate no-op reapply, linked history comparison, and hosted lint because the user explicitly requested online Supabase only. No hosted database reset was performed.
- **Remaining:** P2-03 private storage and signed-preview controls are next; role policies and two-tenant proof follow in P2-04.

## 2026-09-05 — P2-03 hosted private document vault

- **Work unit IDs:** P2-03
- **Outcome:** Created the private `application-documents` hosted bucket with a 10 MiB ceiling, PDF/JPEG/PNG allowlist, safe-filename rule, exact organization/application/document/filename path ownership, and role-aware read/write policies. SMEs can change evidence only before review advances; buyers and funders gain read-only evidence access only at eligible workflow states.
- **Changed areas:** `supabase/migrations/20260905023000_private_document_storage.sql`, `tests/integration/storage-security.mjs`.
- **Failed check retained:** The first hosted test attempted Supabase's newer secret-key format, which the installed Auth admin client rejected as an invalid API key. The runner was switched to the project's legacy service-role key without displaying or persisting either credential.
- **Hosted evidence:** Migration dry-run and apply passed; linked database lint reports zero errors. The experimental CLI storage listing returned `application-documents/`. A synthetic authenticated SME uploaded a valid PDF, while a public URL failed. A one-second signed preview loaded while valid and failed after expiry. Malformed paths, `text/plain`, and a 10 MiB + 1 byte upload were each rejected.
- **Cleanup evidence:** The integration probe removes its object, application, organization, profile/auth user and also removes exact-name stale probe records from interrupted prior runs. A second complete run passed with cleanup in place.
- **Security boundary:** Credentials are obtained transiently from the authenticated CLI, injected only into the test process, and never displayed, written to a file, or committed. All test identities use randomized `example.invalid` addresses and Demo-labelled data.
- **Remaining:** P2-04 must define and prove table-level role/tenant policies across select, insert, update, and delete.

## 2026-09-05 — P2-04 hosted tenant isolation

- **Work unit IDs:** P2-04
- **Outcome:** Added server-enforced role/organization helpers and explicit RLS policies for all eleven business tables. SMEs can read their own tenant and mutate only editable drafts; buyers see and decide only requests addressed to their organization; funders see only buyer-confirmed or later packages; verification checks and audit events expose no ordinary-client write policy.
- **Changed areas:** `supabase/migrations/20260905031500_tenant_rls_policies.sql`, `supabase/migrations/20260905032000_nullable_identity_constraints.sql`, `tests/integration/rls-two-tenant.mjs`, regenerated `src/types/database.ts`.
- **Schema correction:** Pre-test review found the initial `NULLS NOT DISTINCT` constraints would permit only one no-registration organization per kind and one no-invoice draft per owner/buyer pair. A corrective hosted migration replaced them with partial unique indexes that enforce identity only when the identifying value exists.
- **Failed checks retained:** The first policy run used an already occupied invoice document slot; the test now uses the purchase-order slot. The second expected a denied update to return an error, while PostgREST safely returned zero affected rows; the final assertion now proves zero rows changed and independently verifies the stored check remains unchanged. The nullable-constraint migration initially used an assumed/truncated generated constraint name twice; it was replaced by catalog lookup and then applied successfully.
- **Hosted evidence:** Both policy and constraint migrations applied successfully and linked database lint reports zero errors. The final randomized two-tenant suite passed twice from clean synthetic users. It proves own-tenant select/insert/update/delete succeeds, cross-tenant ID substitution returns no rows or a policy violation, addressed buyers cannot view/decide another buyer's request, funders cannot view draft or buyer-pending packages, and ordinary users cannot rewrite/delete verification facts or fabricate audit events.
- **Cleanup/security:** Every run deletes its applications, organizations, profiles, and randomized `example.invalid` auth users. Project credentials are injected transiently from the authenticated CLI and are never displayed, persisted, or committed. Fresh generated TypeScript types exactly match the hosted schema.
- **Remaining:** P2-05 authentication clients and server authorization helpers are next.

## 2026-09-05 — P2-05 hosted authentication and authorization helpers

- **Work unit IDs:** P2-05
- **Outcome:** Replaced the fake role cookie with Supabase SSR browser/server clients, Next.js request proxy refresh, verified-user lookup, role and tenant DAL guards, live credential sign-in, hosted one-click Demo sign-in, and sign-out. Protected layouts repeat the server role check after the optimistic proxy check; application access is looked up through RLS and invalid/wrong-tenant IDs receive the same not-found response.
- **Changed areas:** `src/lib/supabase`, `src/lib/auth`, `src/proxy.ts`, protected layout/dashboard, login action/form, app shell, browser tests.
- **Documentation basis:** Read the installed Next.js 16 authentication, proxy, cookies, and redirect guides plus installed `@supabase/ssr` guidance. Current official Supabase SSR guidance was checked: the proxy uses `getClaims()` rather than trusting `getSession()`, while the DAL uses the fresh network-backed `getUser()` requested by this unit.
- **Failed checks retained:** The first production build rejected a non-function export from a `use server` module; initial state moved to the client component. One asynchronous form unit test queried before the action state settled and now awaits the accessible result. The first browser suite still expected unauthenticated dashboard access and the deleted fake cookie; tests now authenticate before protected checks and verify a real Supabase cookie. A later offline-state check started before the streamed dashboard finished; the shared login helper now waits for `#main-content`.
- **Automated/browser evidence:** Final lint, typecheck, 16 unit assertions, and production build passed. Ten serial Chromium checks passed, including signed-out redirect, generic invalid-credential messaging, sign-out followed by denied re-entry, 390/768/1440 layouts, and three role dashboards in separate browser contexts.
- **Security boundary:** No client code receives the demo password or service role. Browser authorization is never trusted: proxy, DAL, mutation helper, and database RLS form independent layers. Live authentication failures do not reveal whether the email or password was wrong.
- **Remaining:** Dashboard data is still the explicitly named temporary repository until P2-07.

## 2026-09-05 — P2-06 reproducible synthetic hosted identities

- **Work unit IDs:** P2-06
- **Outcome:** Added rerunnable SQL for fixed Demo SME, buyer, and funder organizations and a rerunnable admin provisioning script for the corresponding fictional profiles/auth users/memberships.
- **Changed areas:** `supabase/seed.sql`, `scripts/provision-demo-users.mjs`, environment template/schema, demo login action, package script.
- **Hosted evidence:** The seed applied to the linked project. The provisioning script passed twice, creating on first run and safely updating the same three identities on rerun. All names include Demo context at the organization or account workflow level, and addresses use the reserved `.example` domain.
- **Secret handling:** The generated password exists only in ignored `.env.local`; the provisioning service credential was injected transiently from the authenticated CLI. Neither appears in repository files, command output, browser output, or Git.
- **Remaining:** P2-07 must add representative synthetic application rows and replace hard-coded dashboard metrics/queues with typed RLS-filtered Supabase queries.

## 2026-09-05 — P2-07 live dashboards and Phase 2 gate

- **Work unit IDs:** P2-07; Phase 2 gate
- **Outcome:** Deleted the temporary demo session/repository and replaced it with a typed server-only Supabase dashboard repository. SME metrics and current application, buyer confirmation queue, and funder buyer-confirmed queue are calculated from hosted RLS-filtered rows. Empty collections render explicit empty states; query failures throw generic errors into the existing protected error boundary; route loading remains geometry-stable.
- **Seed expansion:** The safe provisioning script now idempotently upserts four Demo organizations, three hosted Demo identities/memberships, four synthetic applications, confirmation states, and explainable check results. No bank, government identity, real customer, or real email data is used.
- **Failed check retained:** After `supabase/seed.sql` changed, the hosted CLI updated its recorded seed hash but did not insert the new fourth organization; the following application upsert correctly failed its foreign key. Provisioning now upserts the same organization fixtures before dependent records, passed twice consecutively, and no longer relies on ambiguous remote seed-hash behavior.
- **Automated evidence:** Final lint, typecheck, 16 unit assertions, production build, and ten Chromium tests passed. Hosted database lint reports no errors. The randomized RLS suite passed select/insert/update/delete isolation; the storage suite passed private/public, expiry, path, MIME, and size checks; provisioning reran successfully.
- **Role/browser evidence:** Three clean browser contexts separately entered SME, buyer, and funder accounts. Each received its expected heading and a role-specific hosted row; each was explicitly checked not to render a row outside its permitted workflow/tenant. Signed-out redirect, generic bad-login error, real session cookie, sign-out, and denied post-sign-out re-entry also passed.
- **Secret evidence:** `.env.local` is ignored. Client static assets contain neither the local demo-password value nor server-only environment names. Repository working files contain no `sb_secret_` value or assigned demo password. Service credentials were transient only.
- **Phase 2 result:** All P2-01 through P2-07 acceptance checks are complete against the linked hosted Supabase project. No local Supabase service was used.
- **Remaining:** Phase 3 begins with the SME application draft form and secure three-document upload workflow.

## 2026-09-05 — P3-01 private SME application drafts

- **Work unit IDs:** P3-01
- **Outcome:** Added a protected `/applications/new` business step for selecting the Demo buyer, entering the purchase-order reference, invoice identity/value/currency and expected payment date, and explicitly consenting to AI document processing. The server repeats the SME role check, validates every field, verifies the buyer through RLS, creates only an SME-owned `draft`, and redirects to a private application summary.
- **Changed areas:** hosted application-detail migration and generated database types; application validation, server action, form, new/detail routes; protected navigation and SME call to action; Demo cleanup/provisioning; unit, integration, and browser tests.
- **Failed checks retained:** The first browser assertion matched both the route alert and the form error-summary alert; it now scopes to the form summary. The initial insert used `INSERT ... RETURNING`, which the current RLS visibility policy rejected even though the insert itself was permitted. The action now assigns a server-generated UUID, inserts without a returning clause, and redirects with that UUID; a hosted authenticated round-trip independently proves the row is readable by its SME owner.
- **Automated evidence:** Final lint and typecheck passed; seven Vitest files with 23 assertions passed; the Next.js production build passed. Twelve serial Chromium checks passed, including linked invalid-field errors, a successful hosted SME draft creation, and generic buyer denial. A hosted authenticated integration test created, read, asserted, and removed an exact-value consented draft. Linked hosted database lint reports no schema errors, and Demo provisioning/cleanup passed after the browser suite.
- **Manual visual evidence:** Inspected the live route at desktop width and at 390 × 844. The desktop form has a clear hierarchy and compact two-column detail grid; mobile collapses to one column with no horizontal overflow and keeps the four-item bottom navigation visible. The purchase-order input exposes a visible three-pixel focus ring, and the consent explanation remains adjacent to its control.
- **Security/demo boundary:** Only hosted Supabase is used. Buyer choices are RLS-visible Demo organizations; client input cannot choose the owning SME or creator; consent time is written server-side; wrong roles and tenant IDs receive a generic not-found response. All money movement and finance decisions remain explicitly simulated.
- **Remaining:** P3-02 is next: a five-step Details → Documents → Review → Verification → Buyer task layout whose progress comes from hosted server data.

## 2026-09-05 — P3-02 server-derived guided application journey

- **Work unit IDs:** P3-02
- **Outcome:** Added the five-step Details → Documents → Review → Verification → Buyer journey to both draft creation and the private application route. Desktop uses a sticky progress card beside the task panel; mobile uses `Step n of 5`, an accessible progress bar, the current task, and the next locked prerequisite. Completed/current/locked states come from the hosted application status plus RLS-visible document categories on every request.
- **Changed areas:** pure application-progress model, responsive task-layout component, new/detail application routes, shared heading containment, unit tests, and hosted browser flow.
- **Failed checks retained:** The first typecheck revealed that conceptual status names from an early planning note did not match the generated hosted enum. The progress model was corrected to use the actual `documents_uploaded`, `fields_extracted`, `sme_reviewed`, and `checks_complete` sequence. Visual QA then found global landing-page `h1` CSS leaking into protected mobile pages; the landing typography is now scoped to `.hero h1`, and application identifiers use safe wrapping.
- **Automated evidence:** Final lint and typecheck passed; nine Vitest files with 30 assertions passed, covering pre-draft, saved-details, three-document, reviewed, checked, and buyer-confirmed states plus locked explanations. The production build passed. Twelve serial Chromium checks passed; the hosted SME flow creates a draft, confirms Documents is current and later steps are locked, then proves the same `1 of 5 complete` result after both refresh and browser back. Demo cleanup passed afterward.
- **Manual visual evidence:** Inspected the hosted-data route at 1280px and 390 × 844. Desktop shows the five-step sticky rail and a 720px task panel; mobile shows a compact progress card and bottom navigation. Both report zero horizontal overflow. Long invoice references wrap cleanly at 48px desktop and 36px mobile heading sizes.
- **Security/state boundary:** No browser-local progress flag exists. Details are complete only from stored application fields and server-written consent; document progress uses RLS-visible document categories or a later persisted application status. Locked controls remain genuinely disabled and explain the prerequisite.
- **Remaining:** P3-03 is next: the three named private upload slots with type/size validation, progress, cancel/retry/remove, and signed preview.

## 2026-09-05 — P3-03 private resumable document slots

- **Work unit IDs:** P3-03
- **Outcome:** Replaced the document placeholder with one named slot each for purchase order, delivery evidence, and invoice. Every slot explains its purpose, supports drag/drop and browse, validates PDF/JPEG/PNG plus a 10 MiB ceiling, sanitizes the storage filename, reports real resumable progress, and provides cancel, retry, remove, page count, and private preview controls.
- **Hosted architecture:** A server action first registers an RLS-owned document/path. The browser then sends bytes directly to Supabase Storage's TUS endpoint in fixed 6 MiB chunks using the current access token only as a credential. Finalization downloads the private object server-side, checks byte count and magic signature, reads PDF page count, writes a completion timestamp, and advances the application only after all three categories are complete. Preview uses a 60-second signed URL; no public object URL is generated by the app. This follows the official Supabase resumable-upload and private-bucket guidance.
- **Changed areas:** upload-completion migration and regenerated hosted types; document validation, upload reducer, server actions, three-slot client panel, application data query/progress derivation, dependencies, cleanup script, storage integration tests, and full hosted browser flow.
- **Failed checks retained:** TypeScript initially could not narrow combined success/shape checks from server-action unions; success discrimination and payload validation are now separate. The first three-file browser run exceeded the test's original 30-second total while the third slot was preparing, so only that full hosted workflow receives a 120-second budget. A PDF-preview navigation assertion proved brittle because Chromium's viewer aborts the navigation after the signed request begins; the browser now asserts the completed Preview control while the hosted storage test directly proves signed loading and public denial. That storage test's one-second expiry window also proved too narrow under network variance and now uses three seconds followed by a 4.5-second expiry check.
- **Automated evidence:** Final lint and typecheck passed; ten Vitest files with 34 assertions passed. The production build passed. Twelve serial Chromium checks passed, including wrong type, oversize, three real hosted TUS uploads, page counts, Review unlocking, removal, refresh, and browser-back persistence. The authenticated storage suite rejects a duplicate slot, malformed path, wrong MIME, and 10 MiB + 1 byte; it also proves public failure, signed preview success, expiry failure, and cleanup. Hosted draft round-trip and linked database lint also pass.
- **Manual visual evidence:** Inspected the live RLS-backed state at 1280px and 390 × 844. Desktop keeps the sticky journey beside three calm, clearly separated evidence cards. Mobile stacks the drop zone and uploaded receipts with readable Preview/Remove controls and the persistent bottom navigation. Both widths report zero horizontal overflow.
- **Security/recovery boundary:** Storage remains private; RLS binds organization/application/document IDs to the object path. The server repeats SME ownership and editable-status checks for prepare, finalize, and remove. MIME labels alone are not trusted. Interrupted rows can be replaced safely, TUS resumes a known fingerprint, terminal validation failures clear the object/row, and E2E cleanup removes storage objects before application rows. Server-computed hashes are groundwork only; P3-04 remains unchecked until exact-duplicate rule `V009` and cross-tenant disclosure tests are complete.
- **Remaining:** P3-04 is next: formalize trusted SHA-256 duplicate handling as `V009`, including same bytes under another filename and non-disclosure across tenants.

## 2026-09-05 — P3-04 trusted exact-duplicate rule V009

- **Work unit IDs:** P3-04
- **Outcome:** Formalized exact-file rejection as deterministic rule `V009`. Finalization downloads the private object in the authenticated server workflow, computes SHA-256 from those bytes, checks only RLS-visible completed documents in the same SME organization, and persists the hash only after signature/page validation. A renamed file with identical bytes is removed before extraction and returns a plain `V009 · Exact duplicate file` message.
- **Database boundary:** Added a least-privilege `security definer` function that can write the otherwise immutable verification run/check only when the caller is an authenticated SME on an editable owned application and the hash already exists in that same organization. It records only the attempted filename and generic `same_organization` scope—never another application's or document's identity. Calls with a hash that exists only in another tenant fail as if no duplicate exists.
- **Changed areas:** hosted V009 migration and regenerated types, trusted duplicate comparison/finalization action, renamed-file browser scenario, and randomized two-tenant hosted integration suite.
- **Automated evidence:** Final lint and typecheck passed; ten Vitest files with 34 assertions passed; the production build passed. Twelve serial Chromium checks passed, including real upload of one file followed by the same bytes under a different filename, visible V009 rejection, and successful recovery with a different file. The hosted two-tenant suite proves a persisted `V009 fail`, no document admitted to the attempted application's extraction set, the same hash accepted for Tenant B, zero Tenant-B hash rows visible to Tenant A, and a cross-tenant RPC probe rejected. Linked hosted database lint reports no errors and all probes/E2E objects were cleaned.
- **Race/failure behavior:** The normal pre-update lookup records V009 before removing the attempted object. The unique-index race fallback also records V009 before reporting that code; if immutable check recording fails, it returns only a generic finalization error rather than claiming a V009 record exists.
- **Security boundary:** No client-supplied digest is trusted. Hash matching is exact and organization-scoped, the object remains private, and no conflicting filename, path, document ID, application ID, organization ID, or digest is disclosed to the uploader.
- **Remaining:** P3-05 is next: reopen drafts at the first incomplete server-derived step and prove sign-out/sign-in recovery without showing deleted or failed uploads as complete.

## 2026-09-05 — P3-05 resilient draft recovery and Phase 3 gate

- **Work unit IDs:** P3-05; Phase 3 gate
- **Outcome:** The private application route now presents the stored buyer, purchase-order reference, invoice value, expected payment date, and AI-processing consent alongside the saved evidence. Every reopening derives the first incomplete task from hosted application and completed-document rows. When all three evidence categories are complete, Review becomes the next task and a clear ready-for-review panel appears; no browser-local progress flag is used.
- **Recovery evidence:** The hosted Chromium journey uploads all three synthetic PDFs, proves a renamed exact duplicate remains failed and does not increase the completion count, removes the purchase order, reloads, navigates away and back, signs out, signs in with a fresh hosted session, and reopens the same URL. The two surviving filenames and values return, the removed slot remains `Required`, and progress returns to Documents at `1 of 5 complete`.
- **Manual visual evidence:** Inspected the recovered hosted draft at 1280 × 900 and 390 × 844. The saved-information card, current task, missing slot, two completed receipts, and mobile navigation remain readable, with document controls available and zero horizontal overflow at both widths.
- **Automated evidence:** Final lint, typecheck, unit, production build, complete browser suite, hosted draft/storage/duplicate integrations, linked database lint, migration dry-run, cleanup, diff hygiene, and secret scan passed. Only the linked hosted Supabase project was used.
- **Phase 3 result:** P3-01 through P3-05 are complete. An SME can create a private draft, upload and manage all three evidence categories, recover from validation/duplicate/removal paths without losing valid data, and resume after a new authenticated session.
- **Remaining:** Phase 4 begins with versioned Gemini extraction schemas and adversarial prompt fixtures; extraction remains unavailable until its server adapter and mandatory human-review path are complete.

## 2026-09-05 — P4-01 versioned extraction contract

- **Work unit IDs:** P4-01
- **Outcome:** Added one strict `extraction-v1` Zod contract and one fixed prompt for each purchase-order, delivery-evidence, and invoice category. Every candidate contains a value, confidence in basis points, and visible source label; missing or ambiguous facts must use `null`, zero confidence, and no source label. Dates require real ISO calendar dates, currencies require three uppercase letters, and money candidates require unformatted unsigned decimals for later deterministic normalization.
- **Safety boundary:** Every prompt treats document instructions, links, role changes, and requested output formats as untrusted data; forbids tools, financing decisions, extra fields, outside knowledge, and placeholders; and states that a human reviews every result. Strict schemas reject unknown properties, including an attempted `approveFunding` action.
- **Automated evidence:** Lint and typecheck passed. Eleven Vitest files with 48 assertions passed. Extraction fixtures cover a complete purchase order, missing delivery facts, unknown fields, inconsistent null/confidence metadata, blank source evidence, impossible and non-ISO dates, invalid currency, formatted money, and an adversarial instruction embedded in document text.
- **Failure corrected:** Tightening the reusable candidate schema initially exposed a TypeScript generic-inference error for its `value` property. The refinement now narrows the candidate shape explicitly while retaining each concrete Zod value schema; lint, typecheck, and the complete unit suite then passed.
- **Remaining:** P4-02 must use this contract in a server-only Gemini Flash adapter, validate live model output again, persist provider metadata and normalized candidate fields idempotently, and never expose the API key.

## 2026-09-05 — P4-02 server-only Gemini extraction adapter

- **Work unit IDs:** P4-02
- **Outcome:** Added a server-only Gemini adapter using the installed `@google/genai` SDK and stable `gemini-2.5-flash-lite`. It sends an authorized private PDF/JPEG/PNG inline with the category-specific fixed prompt, requests JSON constrained by an explicit supported response schema, applies a 20-second timeout capped at 30 seconds, retries at most once only for transient timeout/rate/server failures, parses JSON, and revalidates it with the stricter Extraction v1 Zod contract.
- **Hosted persistence:** Applied the hosted extraction migration, adding document-level schema/provider/model/raw-response/safe-metadata/completion/failure receipts and per-field source labels. A service-role-only transaction replaces the document's entire candidate field set, stores raw and normalized values plus confidence/provenance, advances the application only after all three categories are extracted, and records a non-sensitive audit event. Authenticated and anonymous roles have no execute permission.
- **Automated evidence:** Lint, typecheck, 12 Vitest files with 54 assertions, and the production build passed. Adapter tests prove inline bytes and structured schema, sanitized metadata, invalid JSON/wrong-category/extra funding-action rejection without retry, one bounded transient retry, and timeout behavior. The hosted integration proves a browser-authenticated SME cannot invoke the writer and two trusted saves produce exactly one ten-field invoice set. Linked database lint reports no errors.
- **Secret evidence:** A production browser-bundle scan found no `GEMINI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, or privileged persistence RPC identifier. Provider exceptions are converted to fixed safe codes/messages; API keys and raw provider errors are never logged or returned.
- **External-service status:** No `GEMINI_API_KEY` is currently configured, so no live Gemini response is claimed. The adapter was verified with controlled provider responses and the real hosted persistence path. The Phase 4 gate remains open until a synthetic live call succeeds or this missing-key block is explicitly documented alongside the honest P4-03 fallback.
- **Documentation basis:** Current official Google GenAI guidance was checked for server-side key handling, inline document data, `responseJsonSchema`, response MIME type, and abort signals. A stable Flash-Lite model was selected for the hackathon's low-cost extraction workload.
- **Remaining:** P4-03 must add an explicit, visibly labelled Demo extraction mode keyed only to known bundled synthetic document hashes; it must never activate automatically after a live-provider failure.

## 2026-09-05 — P4-03 honest hash-bound Demo extraction

- **Work unit IDs:** P4-03
- **Outcome:** Added three polished one-page synthetic evidence PDFs representing one consistent Demo transaction and deterministic Extraction v1 fixtures bound to their exact server-computed SHA-256 hashes and document categories. `PROOFFLOW_EXTRACTION_MODE` defaults to `live`; only the explicit `demo` value selects fixtures, and the live-provider error path never calls the fallback.
- **Honesty boundary:** Unknown bytes, a known sample uploaded into the wrong category, and any fallback call while `live` is selected return fixed failures without invented values. Demo results persist provider `proofflow-demo-fixture`, model `deterministic-fixture-v1`, the exact fixture hash, and the disclosure `Demo extraction—not processed by live AI`. The Review-ready panel displays that same disclosure only in Demo mode.
- **Hosted/database evidence:** Applied a linked hosted migration that allows the trusted extraction transaction to record either the Google or explicit Demo provider while preserving service-role-only execution. Hosted database lint reports no errors, and the retry-idempotent extraction integration passes against the updated signature.
- **Automated evidence:** Lint, typecheck, 14 Vitest files with 59 assertions, and the targeted hosted Chromium journey passed. The browser uploaded the three actual bundled PDFs, reached Review, and asserted the exact Demo disclosure. Unit tests prove exact known-hash success, explicit-mode enforcement, unknown-hash rejection, wrong-slot rejection, and live-mode label absence.
- **Visual evidence:** Rendered and inspected all three A4 PDFs; headings, evidence tables, totals, receipt signature, and synthetic-data footers are legible with no clipping or overlap. The Review panel was inspected at desktop and 390 × 844; the disclosure, disabled next action, saved values, and three completed file receipts remain readable with zero horizontal overflow.
- **Failure retained:** A separate visual draft initially rejected the delivery sample as V009 because the earlier recovery test intentionally left the same bytes in the Demo organization. Cleaning only those E2E objects and re-authenticating produced the expected three-document Review state, confirming the duplicate boundary remained active.
- **Remaining:** P4-04 must make extraction an enabled user action and present signed document preview beside editable candidate fields, including mobile switching, missing/low-confidence warnings, and preserved edits.

## 2026-09-05 — P4-04 extraction action and human-review workspace

- **Work unit IDs:** P4-04
- **Outcome:** Added an SME-only extraction action that requires the owned application to contain exactly three completed evidence categories, processes them in a fixed order, and advances only through the trusted extraction transaction. The resulting Review workspace exposes all 21 Extraction v1 candidates in purchase-order, delivery, and invoice tabs with editable values, confidence guidance, category and field provenance, original document text, explicit normalization formats, and an edited-value receipt.
- **Private preview:** Each document is displayed through a five-minute signed URL from the private hosted bucket. An expired or unavailable preview fails closed with reload guidance. On constrained layouts, the user switches between Document and Fields while edits remain in component state; wider layouts show both together.
- **Honesty and human-control boundary:** The exact Demo disclosure remains visible after extraction. Low-confidence and missing candidates are marked `Please check`, while all values remain suggestions. The save control remains deliberately unavailable until P4-05 supplies the audited server-side review transaction; extraction itself cannot mark a field reviewed.
- **Real hosted evidence:** In the in-app browser, created a new private SME application, uploaded the three bundled PDFs, ran explicit Demo extraction against hosted storage, observed the application advance to `fields_extracted`, and reviewed all 21 persisted fields with signed preview access. The visual check caught cramped medium-width columns; the workspace now uses its single-column Document/Fields switch until enough width exists, and tabs are ordered PO → delivery → invoice.
- **Automated evidence:** Lint and typecheck passed; 15 Vitest files with 61 assertions passed; and the production build passed. Component tests prove provenance, missing-field guidance, normalization hints, explicit Demo disclosure, expiring-preview behavior, and edit preservation across document and mobile-view switches.
- **Remaining:** P4-05 must validate and save the complete reviewed record transactionally, record editor provenance and an audit event, reject unreviewed verification, and make repeat submission idempotent.

## 2026-09-05 — P4-05 atomic human review and Phase 4 gate

- **Work unit IDs:** P4-05; Phase 4 gate
- **Outcome:** The Review workspace now submits all 21 displayed candidates through one SME-only server action. Text, real ISO dates, ISO currency, decimal money, and the delivery signature boolean are validated before a single hosted transaction records accepted versus corrected status, reviewer identity, review time, reviewed document state, application state, and one non-sensitive audit receipt.
- **Bypass protection:** Removed direct browser updates to individual document fields. The trusted transaction locks the application, confirms the actor is an SME member of the owner organization, requires the exact Extraction v1 document/field set and every field ID exactly once, and rejects missing, duplicate, foreign, blank, malformed, or post-approval changed submissions. AI extraction has no path that can populate reviewer identity or advance the application to `sme_reviewed`.
- **Idempotency:** Replaying the identical complete review returns a replay receipt without changing provenance or creating another audit event. Replaying different values after approval is rejected, preserving the reviewed record for verification.
- **Hosted evidence:** A disposable hosted integration fixture proved 20-of-21 rejection, blocked direct per-field update, buyer-role rejection, atomic 21-field approval, one deliberately corrected value, 21 reviewer receipts, three reviewed documents, application transition to `sme_reviewed`, exactly one audit event, identical replay, and changed-replay rejection; all fixture rows were removed afterward. Linked database lint reports no errors.
- **Visible journey evidence:** Submitted the 21-field review from the real hosted browser application. The journey advanced from `2 of 5 complete` to `3 of 5 complete`, Review became complete, and Verification became the current unlocked task.
- **Phase 4 result:** P4-01 through P4-05 are complete. No live Gemini call is claimed because no API key is configured; the stable server adapter is tested, while the explicit exact-hash Demo provider remains visibly disclosed. Malformed or unreviewed data cannot cross the human-review boundary.
- **Remaining:** Phase 5 must run deterministic cross-document checks only from the frozen SME-reviewed record and present an explainable, non-score verification report.

## 2026-09-05 — P5-01 through P5-06 deterministic verification and SME handoff

- **Work unit IDs:** P5-01, P5-02, P5-03, P5-04, P5-05, P5-06; Phase 5 gate
- **Outcome:** Added pure, locale-independent normalization for names, references, real ISO dates, ISO currency codes, and exact decimal-to-minor-unit conversion. A frozen `verification-v1` engine now runs V001–V012 over the complete human-reviewed record and produces typed pass/review/fail checks with severity, compared values, explanations, and source document IDs. The overall result follows fail-before-review-before-pass and describes evidence consistency only.
- **Hosted persistence:** Applied a forward-only hosted migration adding normalized supplier invoice identity, database uniqueness, rule version, and overall result. One SME-authorized transaction validates the exact twelve-check payload, validates its computed overall result, requires all 21 human-reviewed fields, inserts a new immutable run and checks, advances the application, and records a non-sensitive audit event. Reruns create new run IDs without rewriting prior evidence; malformed/partial payloads write nothing.
- **Duplicate and handoff boundary:** Normalized invoice identity is scoped to the supplier organization, so another supplier may use the same reference while the same supplier receives V010 fail. The server transaction independently verifies V010 against the database. A separate idempotent SME-only transaction creates exactly one pending buyer confirmation and audit event; a failed latest run is blocked, while a review-only pack may proceed with its warnings visible.
- **Interface:** The private application route now presents a calm verification summary, all twelve explainable checks, compared values, pass/review/fail text and icons, evidence links, correction guidance, rerun control, and one clear buyer-handoff action. Desktop uses a compact two-column result grid; mobile retains a single readable flow. The waiting state removes repeat-send controls and explains that the request is in the buyer organization's secure queue.
- **Failed checks retained:** The first typecheck rejected BigInt literal syntax under the repository's ES2017 target; exact arithmetic now uses the compatible `BigInt()` constructor. The first visual fixture omitted application due-date metadata and used generic rule titles, causing the browser assertion and progress step to disagree with the otherwise rendered report; the fixture now contains a complete application record and the visual assertion targets its deliberate fixture title. The full browser gate then correctly rejected a bundled sample whose exact bytes remained in a real earlier Demo application; the upload test now writes unique valid PDF metadata for each run while deliberately reusing those new bytes only for its named V009 scenario, so it no longer depends on deleting legitimate Demo evidence.
- **Automated evidence:** Final lint and typecheck passed. Eighteen Vitest files with 114 assertions passed, including table-driven normalizers, every logical pass/review/fail rule branch, overall precedence, pre-run, failed, rerun, and post-handoff UI states. The production build passed. A hosted integration proved incomplete-run rollback, exact 12-check persistence, immutable reruns, V010 failure, buyer-role denial, failed-pack send denial, one confirmation, one send audit event, and replay idempotency. Linked hosted database lint reports no errors.
- **Responsive/visual evidence:** Three hosted Chromium checks passed at 390×844, 768×1024, and 1440×900 with zero horizontal overflow. The report, first/last rule, waiting state, two-column desktop grouping, mobile hierarchy, and document source links were visually inspected. Synthetic visual fixtures were removed afterward.
- **Phase 5 result:** The SME can now complete the full evidence preparation journey and securely hand a non-failed, explainable pack to the selected buyer. Phase 6 is next and will implement the user-approved six-question confirmation, conditional dispute explanations, summary, declaration, drawn signature, immutable certificate, and cross-role certificate visibility.

## 2026-09-05 — P6-01 through P6-07 structured buyer confirmation and certificate

- **Work unit IDs:** P6-01, P6-02, P6-03, P6-04, P6-05, P6-06, P6-07; Phase 6 gate
- **Outcome:** Replaced the disabled buyer action with an oldest-first, tenant-filtered queue and a three-stage confirmation journey. The buyer answers six transaction-specific Yes/No questions, explains every No beside the affected fact, reviews a generated confirmation or dispute summary, and can return without losing answers. Only an all-Yes response reaches the declaration and handwritten-signature stage.
- **Identity and transaction boundary:** The browser supplies answers, negative explanations, job title, declaration version, and bounded normalized drawing strokes. One hosted transaction locks the pending request/application, validates the exact payload, derives the representative name, buyer organization, verified authentication email, decision time, approval UUID, and transaction snapshot on the server, then writes the immutable decision, application status, fresh V012 run, and audit event. Direct completed-record updates, changed replays, wrong roles, unverified identities, malformed signatures, and partial decisions fail closed; identical replay returns the original decision.
- **Certificate:** Added an authorized deterministic one-page PDF generated from the frozen database record. It contains the ProofFlow heading, transaction values, six confirmation marks, representative details, actual captured signature strokes, signed time, approval ID, internal verification URL/reference, declaration, and explicit no-payment-guarantee disclaimer. The PDF is a rendered view, not the source of truth.
- **Cross-role interface:** Buyer receipts show every answer, explanation, identity fact, signature, time, approval ID, and both view/download actions. SME dashboard/application views show the completed buyer state and certificate action. The funder queue excludes disputed requests, shows `Buyer Confirmed ✓`, uses only the latest verification run for warnings, and opens the same certificate. A compact top-bar sign-out control keeps account switching available on mobile.
- **Live path evidence:** Against hosted Supabase, the buyer completed all six Yes answers, reviewed the exact generated summary, entered only a job title, drew a mouse signature, and received an immutable receipt with a server-issued approval ID. A second synthetic request selected No for the invoice amount, required and retained the explanation, bypassed the signature stage, and became an immutable dispute. The SME then saw the confirmation and certificate link; the funder saw only the three confirmed packages with working certificate links.
- **Automated evidence:** Lint and typecheck passed with no warnings; 20 Vitest files with 120 assertions passed; the production build passed. The hosted integration proves the signed receipt, twelve-check V012 rerun, single audit event, exact replay, changed-replay rejection, and SME-role denial. Linked database lint reports no errors, migration dry-run is current, and generated TypeScript database types exactly match the hosted schema.
- **Visual evidence:** The buyer queue, six-question layout, summary, declaration, read-only receipt, SME receipt, and funder badge/action were inspected in the authenticated app with no browser errors or horizontal overflow in the constrained layout. The generated A4 PDF was rendered to PNG at 150 DPI and inspected: hierarchy, transaction rows, all six checks, representative block, captured signature, verification reference, approval ID, and disclaimer are legible without clipping or overlap.
- **Failure corrected:** The first V012 transaction inherited an earlier overall `review` even when V012 was the only review item. Overall status is now recomputed from all twelve persisted checks, with a database trigger keeping future V012 insertions consistent. A constrained browser view also exposed that sign-out existed only in the hidden desktop rail; a top-bar control now keeps it available at every breakpoint.
- **Production-switch boundary:** The workflow already uses authenticated hosted records, server/RLS authorization, immutable transactions, and generated certificates. Demo mode affects only synthetic identities/data and the separately labelled extraction provider. A production launch still requires the documented POPIA, retention, independent security, identity/KYC, and signature-enforceability/trust-provider work; this in-app acknowledgement is not represented as a qualified digital signature.
- **Phase 6 result:** The buyer side is complete. Phase 7 is next: finish the funder's read-only evidence review, deterministic simulated-offer calculation, immutable offer/decline, and SME response.

## 2026-09-05 — P7-01 through P7-05 funder review and simulated offer

- **Work unit IDs:** P7-01, P7-02, P7-03, P7-04, P7-05; Phase 7 gate
- **Outcome:** Completed the funder queue, read-only evidence workspace, deterministic offer calculator, immutable funder offer/decline decision, and one-time SME response. The queue excludes pending and disputed packages, surfaces amount, due date, buyer-confirmation time, latest-run warnings, and direct evidence/certificate actions. The evidence page joins the frozen transaction, source documents and reviewed values, V001–V012 explanations, buyer receipt, and application audit timeline without exposing mutation controls.
- **Deterministic offer boundary:** Percentages are converted to integer basis points and all money uses integer minor units with half-up rounding. The UI previews invoice value, gross advance, fee, net simulated disbursement, buyer due date, and expiry; the hosted transaction independently validates the same inputs, snapshots the computed amounts, and refuses negative, excessive, malformed, or past-dated terms. Historical decisions are never recalculated.
- **Immutable hosted workflow:** Applied `20260905140000_simulated_funder_offers.sql`. Direct authenticated insert/update policies were removed; security-definer transactions enforce funder review, one offer or reasoned decline per funder/application, SME-only response, expiry, exact-replay idempotency, changed-replay rejection, forward-only application statuses, and one audit event per transition. The final action records `funded_simulated` while clearly stating that no real approval, agreement, endorsement, disbursement, or money movement occurred.
- **Live path evidence:** In separate authenticated funder and SME sessions, the funder opened the confirmed Ubuntu package, reviewed all twelve checks and signed buyer receipt, started review, and issued an 80% advance with a 2.5% fee. For the R22,400 invoice the UI and database agreed on R17,920 gross, R448 fee, and R17,472 net. The SME reviewed the plain-language flow, opened the confirmation dialog, accepted once, and completed the explicit simulation; the application reached `funded_simulated` and the page displayed `Simulation complete`.
- **Alternative and security evidence:** The hosted integration records a reasoned funder decline on a separate synthetic application and verifies its zero-value immutable receipt. It also proves exact decision/response/funding replays, changed-offer and changed-response rejection, blocked direct table writes, buyer-role denial, server rejection of negative, over-100%, and past-expiry terms, funded application state, and one audit event for each offer, acceptance, simulated completion, and decline.
- **Automated evidence:** Lint and typecheck passed; 21 Vitest files with 131 assertions passed; the production build passed. The hosted funder integration passed, linked database lint reports zero errors, migration dry-run is current, and checked-in TypeScript database types exactly match the hosted schema.
- **Responsive and honesty evidence:** The live funder dashboard, evidence review, offer form, SME terms, acceptance dialog, and completion receipt were inspected in the in-app browser. The constrained offer receipt has no horizontal overflow. Every financing surface uses `Simulated` language and repeats that ProofFlow does not make a credit decision, form a credit agreement, move money, or imply bank endorsement.
- **Production-switch boundary:** Authentication, hosted persistence, RLS, evidence access, deterministic calculations, immutable decisions, and audit history are real application architecture. The financing adapter remains deliberately simulated; production activation still requires a regulated funding provider, underwriting/mandate controls, enforceable agreements, disbursement/settlement rails, reconciliation, compliance review, and operational monitoring.
- **Phase 7 result:** The SME, buyer, and funder core journey is now implemented through the simulated funding milestone. Phase 8 is next: derive the Trust Passport exclusively from immutable completed evidence and link every metric back to its source.

## 2026-09-05 — Protected sidebar alignment correction

- **Outcome:** Corrected a public-site `nav` style that was leaking into the protected app shell and centering sidebar links as disconnected pills. Scoped the public navigation rule to the landing-page header, made every desktop navigation row fill the available rail width, and retained the compact mobile navigation behavior.
- **Visual refinement:** The organization card now wraps long company names instead of truncating them, navigation has a clear `Workspace` label and consistent icon/text alignment, and the synthetic-data disclosure is a compact footer separated from the primary navigation. The always-visible top-bar sign-out action remains the single sign-out control.
- **Evidence:** The authenticated SME dashboard was inspected in the in-app browser at a 1,265 × 720 viewport. The 256px rail contains five equal 223px navigation rows, the active state spans the full navigation column, and the page reports no horizontal overflow. Targeted navigation tests, lint, typecheck, and the production build pass.

## 2026-09-05 — P8-01 through P8-03 Trust Passport

- **Work unit IDs:** P8-01, P8-02, P8-03; Phase 8 gate
- **Outcome:** Added the SME-only `/trust-passport` route and enabled its role navigation. A server-only repository loads only the authenticated SME organization's applications, completed buyer decisions, and completed verification runs through existing RLS, then passes a minimal projection into a pure deterministic metric calculator.
- **Metric integrity:** The Passport counts confirmed and disputed buyer decisions, distinct confirming buyer organizations, dated evidence history, and verified invoice value grouped by ISO currency. Value is included only when the buyer confirmed and the latest completed verification result is `pass` or `review`; missing and failed verification results, pending decisions, drafts, and other tenants are excluded. Unlike currencies are never summed together.
- **Interface:** The populated page presents four plain metrics, a multi-currency breakdown when needed, and a newest-first timeline with direct links to every source application and buyer receipt. The zero-history state explains exactly how evidence appears and offers a working application action. The page prominently states `This is not a credit score or funding guarantee` and contains no score, gauge, prediction, or AI-generated rating.
- **Automatic-update evidence:** The previously completed hosted happy path appears without manual Passport data entry as `INV-2040-DEMO`, a confirmed receipt, and R22,400 of verified value. An older confirmation without a completed verification result remains visible in history but contributes no verified value, preserving both transparency and metric integrity.
- **Security evidence:** A hosted authenticated SME query returned only its organization records and could not see the fixed Mokoena application. The completed-event source matched the two visible history records and the R22,400 verified total. An authenticated buyer navigating directly to `/trust-passport` received the generic not-found boundary without SME data.
- **Automated and visual evidence:** Lint and typecheck passed; 23 Vitest files with 136 assertions passed; the production build includes the dynamic Passport route. Unit coverage proves zero history, source links, pass/review inclusion, failed/missing verification exclusion, distinct buyers, and currency grouping. The hosted Passport integration passed. The authenticated desktop page was inspected with correct active navigation and no horizontal overflow.
- **Phase 8 result:** The complete SME → buyer → funder journey now produces a reusable, explainable evidence history. Phase 9 is next: add the three documented synthetic sample packs, then complete the route-state, responsive, accessibility, motion/performance, and failure-recovery audit.

## 2026-09-05 — P9-01 through P9-06 experience resilience and sample packs

- **Work unit IDs:** P9-01, P9-02, P9-03, P9-04, P9-05, P9-06; Phase 9 gate
- **Sample packs:** Added deterministic `valid`, `mismatch`, and byte-identical `duplicate` evidence packs. Each contains a clearly marked synthetic purchase order, delivery record, invoice, and expected structured data. The mismatch pack deliberately proves V001 and V003–V008 fail, V002/V009/V010 pass, and V011/V012 require review; the duplicate context proves V009 fails. All nine PDFs were rendered and visually inspected at 150 DPI with legible content and no clipping. The unit suite verifies file signatures, completeness, hashes, expected values, and exact deterministic rule output.
- **Route/state result:** Added `docs/UX-AUDIT.md` with the loading, empty, success, error/recovery, permission, and disabled state for every public and core role route. Buyer and funder dashboards now retain immutable decision history as well as pending work. Help and Account are complete protected routes, every role navigation target works, and the header identity opens Account on mobile. Dead notification and placeholder navigation controls were removed; unavailable records are described plainly.
- **Responsive evidence:** Audited public pages and the SME, buyer, and funder core routes at 390×844, 768×1024, and 1440×900 in the authenticated in-app browser. All reported one main landmark and `scrollWidth === clientWidth`; long identifiers wrap, mobile content clears the bottom bar, document/evidence layouts remain readable, and the loading skeleton preserves the final geometry.
- **Accessibility evidence:** Keyboard focus is visible, the mobile account action is reachable, labels and linked error summaries persist, and live regions announce progress/errors. A new Radix dialog interaction test proves focus returns to the trigger. Public header/action targets were raised to at least 44px, the protected not-found boundary was corrected to avoid nested main landmarks, browser zoom at approximately 200% reflowed without overflow, and reduced-motion styling suppresses non-essential reveal motion.
- **Failure rehearsal:** Unit and hosted evidence cover Gemini timeout and malformed output, upload interruption/retry, expired private preview, exact hash duplicate, immutable exact replay versus changed/stale replay, offline warning/reload, database denial, and cross-tenant identifier/hash probes. Messages stay generic where disclosure would reveal provider, record, or tenant details, and every recoverable failure exposes Retry, Reload, Go back, Remove, or Correct as appropriate.
- **Automated gate:** Lint and typecheck passed. Twenty-four Vitest files with 143 assertions passed. The Next.js production build passed and includes all public and protected routes. Hosted buyer confirmation, V012/audit/replay/tamper/role checks passed; hosted funder offer/decline/SME response/math/replay/tamper/RLS/audit checks passed; hosted Trust Passport completed-event/value/tenant-isolation checks passed. `git diff --check` passed.
- **Database note:** No Phase 9 schema or policy change exists. A final optional linked CLI lint/dry-run recheck was unavailable because the locally cached database login password was rejected; the same hosted policies were exercised successfully by all three authenticated integration suites, and the most recent schema-changing Phase 7/8 gates already recorded clean linked lint and dry-run evidence.
- **Remaining limitations:** The live Gemini provider still needs a production server key; electronic-signature enforceability, identity assurance, retention/POPIA review, formal assistive-technology testing, independent security review, regulated underwriting, agreements, money rails, and operational monitoring remain explicit production work. The sample render PNGs were QA intermediates only and are ignored; the nine PDFs and expected JSON are the committed evidence artifacts.
- **Phase 9 result:** All P9 MUST units pass. Phase 10 is the only remaining phase: final suite/CI/security review, deployment verification, three timed rehearsals, judging evidence, clean/tagged repository state, and proof that the deployed commit matches the tag.

## 2026-09-05 — Public landing login action

- **Outcome:** Added a prominent `Log in` action beside `View build` in the public header. It uses the real Next.js `/login` route and remains the primary visible header action on mobile.
- **Responsive evidence:** At desktop width, both actions remain aligned with the central navigation. At 390×844, `View build` is intentionally hidden to preserve the brand and login action; `Log in` remains a 44px touch target, the page has no horizontal overflow, and clicking it reaches the ProofFlow sign-in screen.
- **Automated evidence:** Lint, typecheck, and the production build passed. The browser smoke assertion now requires the public login link to target `/login`.

## 2026-09-05 — Focused role pages, paged buyer questions, and login artwork

- **Outcome:** Replaced dashboard hash links with real protected list pages. SME and funder Applications open `/applications`; buyer Confirmations opens `/confirmations`; buyer History opens `/confirmations/history`; and funder Offers opens `/offers`. Overview pages are now short summaries with two obvious task links instead of repeating every queue and history row.
- **Buyer confirmation:** Stage 1 now shows one question at a time with `Question n of 6`, an accessible progress bar, and Back/Next controls. Answers persist when moving back. A No answer keeps Next disabled until a short explanation is supplied. The existing `Step n of 3` indicator separates Questions, Review, and Signature, while the SME five-step application journey retains its existing numbering.
- **Navigation/security:** Active navigation selects the most specific route so Buyer History is not also announced as Confirmations. Session refresh now covers `/confirmations/:path*` and `/offers/:path*`. Every new page repeats role authorization on the server and loads only hosted RLS-visible data.
- **Simplification:** Shortened buyer, funder, SME, and confirmation guidance. Separate pages use one heading, one count, concise empty copy, and direct record actions. No fake pagination was added to short record lists.
- **Login visual:** Generated one original document-and-verification background with the built-in image-generation path, copied it into the project, and optimized it from PNG to a 52 KB WebP. A dark emerald overlay protects white-text contrast. The decorative background contains no readable text, logo, identity, bank data, or currency details.
- **Browser evidence:** Funder Applications and Offers each opened at their own URL with the correct active item and zero overflow. Buyer Confirmations and History did the same at 390×844. The SME Applications list opened independently with a working Start action. The login background was inspected at 390×844 and 1440×900; it crops cleanly, keeps the story readable, and adds no horizontal overflow. One rapid demo-role switch hit the existing generic `demo-unavailable` state; a bounded retry recovered normally without data leakage.
- **Automated evidence:** Lint and typecheck passed; 25 Vitest files with 144 assertions passed; the production build passed and includes the four new index/list routes. New interaction coverage proves one-question-at-a-time progress, disabled advancement, retained answers, and Back navigation.

## 2026-09-05 — P9C-01 through P9C-04 Demo Coupa evidence integration

- **Work unit IDs:** P9C-01, P9C-02, P9C-03, P9C-04
- **Architecture:** Added one strict canonical evidence model, a provider-neutral `BuyerSystemConnector`, a server-only Demo Coupa connector, and an inert live-Coupa boundary. The application layer consumes the shared interface and does not branch on demo versus future live transport. Gemini is not called for external-system comparisons.
- **Synthetic scenarios:** The Demo Coupa connector supports exact match, missing invoice, amount mismatch, missing receipt, already paid, and disconnected outcomes using synthetic organizations and references. The buyer controls the active demonstration scenario from Account → Business Connections; every affected surface identifies the source as Demo Coupa.
- **Deterministic decision boundary:** Pure C001–C010 rules return pass, review, or fail with explanations and compared evidence. Exact match becomes `buyer_system_verified`; missing/mismatch/receipt differences become `buyer_exception_review`; already paid becomes `buyer_system_blocked`; disconnected creates one existing signed buyer-confirmation request. Coupa evidence never makes the funding decision.
- **Hosted persistence and security:** Applied the connection, mapping, sync-run, immutable snapshot, check, and exception-resolution schema to hosted Supabase. The server retrieves only a safe connection context through a security-definer function, stores a minimised canonical snapshot with a SHA-256 hash, and records correlation IDs and audit events. RLS and column privileges prevent cross-buyer access, browser credential access, snapshot mutation, and premature funder access.
- **Three-role interface:** SME applications can run and understand the buyer-system check without seeing private buyer configuration. Buyer Account shows the Demo connection, and the confirmation queue exposes only differences for resolution. Funder queue/detail surfaces distinguish `Buyer System Verified` from human confirmation and link to system evidence and the correct certificate while retaining the funder's independent offer/decline action.
- **Certificate:** Added an authorized one-page system-verification PDF containing the Demo Coupa source/mode, transaction references, C001–C010 results, retrieval time, evidence hash, verification identifiers, and non-guarantee disclaimer. It contains no representative identity or fabricated signature. A rendered sample was visually inspected and its extracted text was checked.
- **Automated evidence:** The full local release gate passed after implementation: lint, typecheck, 28 Vitest files with 156 assertions, and the Next.js production build. Unit tests cover all six connector scenarios, malformed payload rejection, retry/timeout behavior, rule outcomes, evidence UI, and certificate text. The hosted integration proves safe metadata visibility, credential denial, tenant isolation, eligible funder visibility, snapshot immutability, prerequisite enforcement, audit creation, and idempotent replay. Linked database lint reports no schema warnings.
- **Live-path evidence:** In authenticated hosted sessions, the Ndlovu application completed its reviewed-document prerequisite, ran the exact-match Demo Coupa workflow, persisted all ten checks and the evidence hash, and appeared to the funder as system verified. Buyer Business Connections and exception-queue surfaces opened successfully, and the funder could open the system evidence and certificate route with no browser console errors.
- **Remaining for P9C-05:** Re-run the new Coupa-specific exception-resolution journey at a true 390 × 844 browser viewport using keyboard-only controls. The in-app viewport override did not change the page's reported CSS width, so this acceptance item remains unchecked rather than claiming unproven evidence. After that focused check, record the result and close the P9C gate.
- **Production boundary:** This milestone is real application architecture with hosted authorization, persistence, RLS, deterministic rules, immutable evidence, and audit history. Only the data source is simulated. Live Coupa stays unavailable until a buyer administrator provides an authorised sandbox, instance URL, read-only OAuth credentials, and the documented POPIA, retention, revocation, monitoring, and contractual controls.

## 2026-09-05 — Automated-verification UX and deliberate demo login

- **Naming:** Replaced user-facing `buyer system verification` wording with **Automated verification** across the application status, journey, actions, evidence page, activity history, dashboard summaries, response messages, documentation, and system certificate. `Demo Coupa` remains visible as the synthetic evidence source rather than the feature name.
- **Research basis:** Applied W3C guidance for clearly labelled, autofill-compatible authentication fields and reduced memory/transcription effort. Applied the U.S. Web Design System summary-box guidance by putting the few crucial facts first and moving long audit material behind progressive disclosure.
- **Application simplification:** The application now shows the automated result, source, match count, checked time, buyer action, and disclaimer in one compact summary. Successful C001-C010 items no longer create ten visible cards. V001-V012 are presented as a pass count plus any exceptions; the full document and automated check lists remain available through native keyboard-accessible disclosure controls. Blocking and review items remain visible without expanding anything.
- **Authentication:** The SME, buyer, and funder demo choices now fill the ordinary email and password fields but never submit the form. The selected role is announced, the user can inspect or edit the fields, password reveal remains available, and authentication occurs only after the user presses **Sign in**. Hosted users continue to use the same form. The exposed password is explicitly limited to dedicated fictional hackathon accounts and is documented as a production removal requirement.
- **Visual evidence:** Inspected the live local login after selecting Buyer and the hosted-data SME application at desktop width. The login clearly showed a selected role, filled email, masked password, and separate Sign in action. The application showed one concise `Automated verification complete` card, three key facts, no-action message, two direct actions, and a collapsed `View all 11 document checks` control instead of the former dense card wall.
- **Automated evidence:** Lint, typecheck, all 28 Vitest files with 157 assertions, and the Next.js production build pass. New coverage proves demo selection fills both fields without invoking authentication, sign-in remains explicit, simplified automated evidence keeps the certificate boundary, and renamed journey/document-check states remain derived correctly.

## 2026-09-05 — Role clarity, workspace rail, and semantic status pass

- **Workspace rail:** Removed the duplicate organization card from the top of the desktop rail and combined organization identity, the explicit role, and the synthetic-data disclosure inside one compact Demo workspace card at the bottom. The account header repeats the organization, signed-in person, and role so identity remains visible while working.
- **Role language:** The user-facing `Buyer` label is now **Large customer**, and the finance role is **Funder / Bank**. Internal `buyer` and `funder` role keys, database relationships, RLS policies, and audit identifiers remain unchanged. This is a presentation change, not a permission-model migration.
- **Color and state hierarchy:** SME identity uses green, Large customer uses amber, and Funder / Bank uses blue. SME application badges now use semantic states: verified/funded are green, review/waiting states are amber, declined/disputed/blocked states are red, and drafts remain neutral. Every chip keeps a written label.
- **Sign-in order:** Hosted email/password and the explicit Sign in action now come first. Fictional demo role choices appear below the Sign in button, use the role colors, and only fill the fields for review; they do not authenticate automatically.
- **Visual evidence:** Inspected the desktop login, SME Overview and Applications, Large customer Overview, and Funder / Bank Overview in authenticated hosted sessions. The rail shows Ndlovu Office Supply inside the bottom Demo workspace card; each top bar and rail identifies the correct role; the login has no horizontal overflow; and the application list visibly distinguishes completed, declined, and draft states.
- **Automated evidence:** `git diff --check`, lint, typecheck, all 28 Vitest files with 157 assertions, and the Next.js production build passed. Existing role-session tests were updated for the new public labels while preserving the same hosted authentication and authorization behavior.
## 2026-09-05 — Unified non-AI product architecture

- **Product:** Replaced AI extraction and consent with a private, manual 21-field evidence-entry workspace and atomic database submission. The existing deterministic V001–V012 verification engine remains the decision-support layer.
- **Trust boundary:** Disabled fabricated interactive customer-system responses. When no authorised ERP connection exists, the application directs the SME to authenticated large-customer confirmation.
- **Compliance and funding:** Added funder-owned external KYC/KYB progress with limited metadata and audit events. Removed the interactive fake-disbursement control; an accepted proposal now waits for an authorised funding-partner confirmation.
- **Commercial model:** Published Core (free), Connect (enterprise/contact us), and Funding Partner positioning, including the legal-review condition on any share of a funder's collected financing fee.
- **Seed strategy:** Consolidated to one application architecture with realistic fictional organizations and optional credentials for testing. Normal product surfaces no longer switch into a sample or demo workflow.
- **Verification:** `npm run lint` passed with no errors or warnings. `npm run typecheck` passed. `npm run test:unit` passed 24 files and 123 tests. `npm run build` completed successfully and generated all 15 pages, including `/` and `/security`.

## 2026-09-05 — Unified architecture final audit

- **Truthful product surfaces:** Removed the remaining simulated-offer and hackathon language from proposal pages, removed the seeded Coupa connection and supplier mappings, made customer-system certificates provider-neutral, and routed enterprise/funder calls to action to a real onboarding-information page.
- **Test and fixture cleanup:** Removed obsolete extraction, Coupa, and fake-disbursement integration tests; updated retained tests to use optional testing credentials and manual-entry fixtures; replaced the old branded output files with realistic fictional evidence packs whose disclaimers remain outside normal product records.
- **Security hardening:** Compliance status updates now require application visibility and reject attempts to take over an application already associated with another funding partner.
- **Responsive fix:** The browser gate found that the public navigation's selector specificity caused a 66px overflow at 390px. The mobile rule now hides the full public navigation correctly.
- **Verification:** ESLint and TypeScript passed. Vitest passed 25 files and 124 assertions. All five Chromium journeys passed. The production build passed and generated 16 pages, including `/contact` and `/security`. All retained integration scripts pass JavaScript syntax checks and `git diff --check` is clean.
- **Deployment boundary:** The manual-evidence, external-compliance, unified-cleanup, funding-boundary, and initializer-fix migrations were subsequently applied to the linked hosted Supabase project. The production release evidence is recorded below.

## 2026-09-05 — Hosted database and Vercel production release

- **Database:** Applied migrations `20260905170000` through `20260905180000` to the linked Supabase project. Corrected the cleanup migration's integration-sync deletion order and the manual-evidence initializer's ambiguous field variable, then verified the hosted initializer creates all 21 required fields.
- **Authentication:** Rotated the three fictional testing-account passwords together and enabled the sample credential chooser only on `/login`. Hosted authentication succeeded for the SME, large-customer, and funder accounts without exposing the password in source or logs.
- **Vercel:** Created and linked the `proofflow` project, connected the GitHub identity required by Vercel, set the framework explicitly to Next.js, and added a deployment ignore list that retains application Supabase modules while excluding local build and QA artifacts.
- **Production result:** Deployment `dpl_8v1jMDkXhP1YFD45PFhA7WMPWubN` reached `READY` and was promoted to `https://proofflow-sepia.vercel.app`. The public login route returned HTTP 200, displayed all three sample credential choices, and a live SME sign-in reached `/dashboard`; the credential panel was absent from the public landing page.
- **Release gate:** ESLint passed with no errors (one warning is confined to an unrelated local deck-build artifact), TypeScript passed, all 25 Vitest files and 124 assertions passed, and the local plus Vercel production builds completed successfully.

## 2026-09-05 — GitHub submission package and judging evidence

- **Repository navigation:** Rebuilt the root README around the live application, five-minute judge path, problem/solution, workflow, responsibility matrix, architecture, security, business model, local setup, repository map and honest MVP limitations. Added a documentation index, contribution contract and private vulnerability-reporting guidance.
- **Judging criteria:** Added a dedicated 100-point evidence map for Innovation and Creativity (20), Technical Implementation (20), Best Use of Tools (15), Security and Data Privacy (15), Business Model (15), and Scalability and Interoperability (15). Every section points to inspectable product or repository evidence and avoids unmeasured impact claims.
- **Visual evidence:** Added current landing-page and security-page screenshots to `docs/assets/`. Excluded an older authenticated screenshot because it contained superseded prototype labels.
- **Commercial evidence:** Documented Core, Connect and Funding Partner offers, the fee-sharing boundary, pilot measurements and a labelled internal six-month implementation range of R750,000–R1,250,000. The estimate excludes lending capital and unknown enterprise licence fees.
- **Copy cleanup:** Replaced remaining visible extraction terminology with manual evidence-entry language, made customer-system exception copy provider-neutral and renamed the login-only helper to `Sample credentials for testing`.
- **Automated gate:** `git diff --check`, ESLint and TypeScript passed. All 25 Vitest files and 124 assertions passed. The Next.js production build passed with 16 generated pages. All six Chromium journeys passed, including mobile overflow, truthful partner routes, security boundaries, signed-out protection, generic authentication errors and login-only sample credentials. The production-dependency audit reported zero known vulnerabilities.
- **Hosted integration evidence:** Hosted application-draft, customer-confirmation and Trust Passport tests passed after aligning their account fixtures with the current login-only test accounts. Service-role integration suites were not rerun because the local release environment intentionally does not retain the production service-role secret; earlier schema/RLS evidence remains recorded above.
