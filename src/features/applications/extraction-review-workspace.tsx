"use client";

import { AlertTriangle, CheckCircle2, FileText, PencilLine } from "lucide-react";
import { useActionState, useMemo, useState } from "react";
import { submitApplicationReview } from "@/app/(protected)/applications/[applicationId]/extraction-actions";
import { Alert } from "@/components/ui/alert";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { cn } from "@/lib/utils";

export type ReviewField={id:string;name:string;sourceValue:string|boolean|null;normalizedValue:string|boolean|null;confidenceBps:number|null;sourceLabel:string|null};
export type ReviewDocument={id:string;kind:"purchase_order"|"delivery_evidence"|"invoice";filename:string;previewUrl:string|null;provider:string|null;fields:ReviewField[]};

const kindLabels={purchase_order:"Purchase order",delivery_evidence:"Delivery evidence",invoice:"Invoice"};
const fieldLabels:Record<string,string>={buyer_legal_name:"Buyer legal name",supplier_legal_name:"Supplier legal name",purchase_order_reference:"PO reference",issue_date:"Issue date",currency:"Currency",order_total:"Order total",delivery_or_completion_date:"Delivery/completion date",receiver_or_signature_present:"Receiver or signature present",invoice_number:"Invoice number",due_date:"Due date",subtotal:"Subtotal",tax:"Tax",total:"Total"};
const formatHints:Record<string,string>={issue_date:"YYYY-MM-DD",delivery_or_completion_date:"YYYY-MM-DD",due_date:"YYYY-MM-DD",currency:"ISO currency code",order_total:"Decimal amount",subtotal:"Decimal amount",tax:"Decimal amount",total:"Decimal amount"};
const inputValue=(value:ReviewField["normalizedValue"])=>value===null?"":typeof value==="boolean"?(value?"Yes":"No"):value;

export function ExtractionReviewWorkspace({documents,applicationId="00000000-0000-4000-8000-000000000000"}:{documents:ReviewDocument[];applicationId?:string}){
  const[selectedId,setSelectedId]=useState(documents[0]?.id??"");const[mobileView,setMobileView]=useState<"document"|"fields">("fields");
  const isDemoExtraction=documents.some(document=>document.provider==="proofflow-demo-fixture");
  const initial=useMemo(()=>Object.fromEntries(documents.flatMap(document=>document.fields.map(field=>[field.id,inputValue(field.normalizedValue)]))),[documents]);
  const[values,setValues]=useState<Record<string,string>>(initial);const selected=documents.find(document=>document.id===selectedId)??documents[0];
  const[state,submitAction,pending]=useActionState(submitApplicationReview.bind(null,applicationId),{status:"idle" as const});
  const reviewedFields=documents.flatMap(document=>document.fields.map(field=>({id:field.id,value:field.name==="receiver_or_signature_present"?values[field.id]==="Yes":values[field.id]??""})));
  if(!selected)return null;
  return <section aria-labelledby="review-fields-heading"><Card><CardContent className="pt-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><span className="text-xs font-bold uppercase tracking-[.12em] text-[var(--primary)]">Human review required</span><h2 id="review-fields-heading" className="mt-2 text-2xl font-bold tracking-[-.03em]">Check every extracted field</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">AI suggests candidate values only. Compare them with the signed private preview and correct anything uncertain.</p></div><span className="w-fit rounded-full bg-[var(--soft)] px-3 py-2 text-xs font-bold">{documents.reduce((sum,document)=>sum+document.fields.length,0)} fields to review</span></div>{isDemoExtraction&&<div className="mt-5 rounded-xl border border-[#d7b45a] bg-[#fff8df] px-4 py-3 text-sm font-bold text-[#654900]">Demo extraction—not processed by live AI</div>}<div className="mt-6 flex gap-2 overflow-x-auto pb-1" role="tablist" aria-label="Evidence documents">{documents.map(document=><button key={document.id} type="button" role="tab" aria-selected={document.id===selected.id} onClick={()=>setSelectedId(document.id)} className={cn("min-h-11 shrink-0 rounded-xl border px-4 text-sm font-semibold transition-colors focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-[var(--focus)]",document.id===selected.id?"border-[var(--primary)] bg-[var(--success-soft)] text-[var(--primary)]":"border-[var(--border)] bg-[var(--surface)]")}>{kindLabels[document.kind]}</button>)}</div><div className="mt-4 grid grid-cols-2 gap-2 rounded-xl bg-[var(--soft)] p-1 xl:hidden"><button type="button" aria-pressed={mobileView==="document"} onClick={()=>setMobileView("document")} className={cn("min-h-11 rounded-lg text-sm font-semibold",mobileView==="document"&&"bg-[var(--surface)] shadow-sm")}>Document</button><button type="button" aria-pressed={mobileView==="fields"} onClick={()=>setMobileView("fields")} className={cn("min-h-11 rounded-lg text-sm font-semibold",mobileView==="fields"&&"bg-[var(--surface)] shadow-sm")}>Fields</button></div><div className="mt-5 grid gap-6 xl:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]"><div className={cn("min-w-0",mobileView!=="document"&&"hidden xl:block")}><div className="mb-3 flex items-center justify-between gap-3"><div><h3 className="font-bold">{kindLabels[selected.kind]}</h3><p className="mt-1 break-all text-xs text-[var(--muted)]">{selected.filename}</p></div><span className="flex items-center gap-1 text-xs font-semibold text-[var(--primary)]"><FileText className="size-4" aria-hidden="true"/>Private preview</span></div>{selected.previewUrl?<iframe src={selected.previewUrl} title={`${kindLabels[selected.kind]} private preview`} className="h-[620px] w-full rounded-xl border border-[var(--border)] bg-white"/>:<div className="grid min-h-72 place-items-center rounded-xl border border-dashed border-[var(--border)] bg-[var(--soft)] p-6 text-center text-sm text-[var(--muted)]">Preview expired or unavailable. Reload this private page to request a new signed preview.</div>}</div><div className={cn("space-y-4",mobileView!=="fields"&&"hidden xl:block")}>{selected.fields.map(field=>{const original=inputValue(field.normalizedValue);const source=inputValue(field.sourceValue);const edited=values[field.id]!==original;const check=field.normalizedValue===null||(field.confidenceBps??0)<8000;return <div key={field.id} className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-4"><div className="flex flex-wrap items-start justify-between gap-2"><label htmlFor={`field-${field.id}`} className="text-sm font-bold">{fieldLabels[field.name]??field.name}</label>{check?<span className="flex items-center gap-1 rounded-full bg-[#fff2d4] px-2 py-1 text-[11px] font-bold text-[#795500]"><AlertTriangle className="size-3" aria-hidden="true"/>Please check</span>:<span className="flex items-center gap-1 text-[11px] font-bold text-[var(--success)]"><CheckCircle2 className="size-3.5" aria-hidden="true"/>Clear candidate</span>}</div><p className="mt-1 text-xs text-[var(--muted)]">Source: {kindLabels[selected.kind]}{field.sourceLabel?` · ${field.sourceLabel}`:" · No visible source"}</p><p className="mt-1 text-[11px] text-[var(--muted)]">Document text: {source||"Missing"}</p>{field.name==="receiver_or_signature_present"?<select id={`field-${field.id}`} value={values[field.id]??""} onChange={event=>setValues(current=>({...current,[field.id]:event.target.value}))} className="mt-3 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 text-sm focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-[var(--focus)]"><option value="Yes">Yes</option><option value="No">No</option></select>:<input id={`field-${field.id}`} required value={values[field.id]??""} onChange={event=>setValues(current=>({...current,[field.id]:event.target.value}))} className="mt-3 min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 text-sm focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-[var(--focus)]"/>}{formatHints[field.name]&&<p className="mt-2 text-[11px] text-[var(--muted)]">Normalized format: {formatHints[field.name]}</p>}{edited&&<p className="mt-2 flex items-center gap-1 text-xs font-bold text-[var(--primary)]"><PencilLine className="size-3.5" aria-hidden="true"/>Edited by you · Original: {original||"Missing"}</p>}</div>})}</div></div>{state.status==="error"&&<Alert tone="error" title="Review not saved" className="mt-6"><p>{state.message}</p></Alert>}<form action={submitAction} className="sticky bottom-20 mt-6 flex flex-col gap-3 rounded-xl border border-[var(--border)] bg-[color:color-mix(in_srgb,var(--surface)_94%,transparent)] p-4 backdrop-blur md:bottom-4 sm:flex-row sm:items-center sm:justify-between"><input type="hidden" name="reviewedFields" value={JSON.stringify(reviewedFields)}/><p className="text-xs leading-5 text-[var(--muted)]">Submitting records your identity and whether each AI candidate was accepted or corrected. This approval cannot be supplied by AI.</p><Button type="submit" loading={pending}>{pending?"Saving human review…":"Save reviewed fields"}</Button></form></CardContent></Card></section>;
}
