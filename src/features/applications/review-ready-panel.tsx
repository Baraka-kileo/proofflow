import { FileCheck2, FlaskConical, LockKeyhole } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";

export function ReviewReadyPanel({extractionMode="live"}:{extractionMode?:"live"|"demo"}){return <Card className="border-[#bfddd2] bg-[var(--success-soft)]"><CardContent className="pt-6"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-4"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-[var(--surface)] text-[var(--primary)]"><FileCheck2 className="size-5" aria-hidden="true"/></span><div><span className="text-xs font-bold uppercase tracking-[.12em] text-[var(--primary)]">Next task · Review</span><h2 className="mt-2 text-xl font-bold">Documents are ready for review</h2><p className="mt-2 max-w-lg text-sm leading-6 text-[var(--muted)]">All three private files are safely stored. Field extraction and the required human review arrive in the next completed build unit.</p>{extractionMode==="demo"&&<p role="status" className="mt-4 flex w-fit items-center gap-2 rounded-full border border-[#d6b870] bg-[#fff7df] px-3 py-2 text-xs font-bold text-[#70520b]"><FlaskConical className="size-4" aria-hidden="true"/>Demo extraction—not processed by live AI</p>}</div></div><Button disabled title="Available after the extraction unit is completed">Start field review</Button></div><p className="mt-5 flex gap-2 border-t border-[#bfddd2] pt-4 text-xs text-[var(--muted)]"><LockKeyhole className="mt-0.5 size-4 shrink-0" aria-hidden="true"/>Verification stays locked until you review every extracted field.</p></CardContent></Card>}
