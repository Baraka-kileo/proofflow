"use client";

import { useActionState } from "react";
import { ArrowRight, LockKeyhole, Sparkles } from "lucide-react";
import { createApplicationDraft } from "@/app/(protected)/applications/new/actions";
import { ErrorSummary } from "@/components/error-summary";
import { Alert } from "@/components/ui/alert";
import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import { FormField } from "@/components/ui/form-field";
import { Input } from "@/components/ui/input";
import { Select } from "@/components/ui/select";
import type { ApplicationDraftState } from "@/lib/validation/application-draft";

const initialState:ApplicationDraftState={errors:[]};
type Buyer={id:string;name:string};

export function ApplicationDraftForm({buyers,minDate}:{buyers:Buyer[];minDate:string}){const [state,action,pending]=useActionState(createApplicationDraft,initialState);const error=(id:string)=>state.errors.find(item=>item.id===id)?.message;return <form action={action} noValidate className="space-y-6"><ErrorSummary errors={state.errors}/>{state.message&&<Alert tone="error" title="Draft not saved">{state.message}</Alert>}<div className="grid gap-5 sm:grid-cols-2"><FormField id="buyerOrganizationId" label="Buyer organization" hint="The organization that will confirm the order and delivery." required error={error("buyerOrganizationId")} className="sm:col-span-2"><Select id="buyerOrganizationId" name="buyerOrganizationId" defaultValue="" aria-describedby={error("buyerOrganizationId")?"buyerOrganizationId-error":"buyerOrganizationId-help"} aria-invalid={Boolean(error("buyerOrganizationId"))}><option value="" disabled>Select the buyer</option>{buyers.map(buyer=><option value={buyer.id} key={buyer.id}>{buyer.name}</option>)}</Select></FormField><FormField id="purchaseOrderReference" label="Purchase-order reference" hint="Use the reference printed on the buyer's order or contract." required error={error("purchaseOrderReference")}><Input id="purchaseOrderReference" name="purchaseOrderReference" placeholder="e.g. PO-DEMO-1042" maxLength={80} aria-describedby={error("purchaseOrderReference")?"purchaseOrderReference-error":"purchaseOrderReference-help"} aria-invalid={Boolean(error("purchaseOrderReference"))}/></FormField><FormField id="invoiceNumber" label="Invoice number" hint="This helps detect accidental duplicate applications." required error={error("invoiceNumber")}><Input id="invoiceNumber" name="invoiceNumber" placeholder="e.g. INV-DEMO-2041" maxLength={80} aria-describedby={error("invoiceNumber")?"invoiceNumber-error":"invoiceNumber-help"} aria-invalid={Boolean(error("invoiceNumber"))}/></FormField><FormField id="invoiceAmount" label="Invoice amount" hint="Enter the full invoice value, including cents." required error={error("invoiceAmount")}><div className="grid grid-cols-[92px_1fr] gap-2"><Select id="currency" name="currency" aria-label="Currency" defaultValue="ZAR"><option value="ZAR">ZAR</option></Select><Input id="invoiceAmount" name="invoiceAmount" inputMode="decimal" placeholder="48750.00" aria-describedby={error("invoiceAmount")?"invoiceAmount-error":"invoiceAmount-help"} aria-invalid={Boolean(error("invoiceAmount"))}/></div></FormField><FormField id="expectedDueDate" label="Expected payment date" hint="When the buyer is expected to settle the invoice." required error={error("expectedDueDate")}><Input id="expectedDueDate" name="expectedDueDate" type="date" min={minDate} aria-describedby={error("expectedDueDate")?"expectedDueDate-error":"expectedDueDate-help"} aria-invalid={Boolean(error("expectedDueDate"))}/></FormField></div><div className="rounded-2xl border border-[var(--border)] bg-[var(--soft)] p-5"><div className="flex gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-[var(--surface)] text-[var(--primary)]"><Sparkles className="size-5" aria-hidden="true"/></span><div><h2 className="text-sm font-bold">Permission to process Demo documents</h2><p className="mt-1 text-xs leading-5 text-[var(--muted)]">ProofFlow will send the three synthetic documents you upload to an AI service only to suggest fields. You review every value, and deterministic rules—not AI—produce the checks.</p></div></div><label htmlFor="aiConsent" className="mt-4 flex cursor-pointer items-start gap-3 rounded-xl bg-[var(--surface)] p-4 text-sm font-semibold"><Checkbox id="aiConsent" name="aiConsent" value="on"/><span>I understand and consent to AI processing for this hackathon Demo.</span></label>{error("aiConsent")&&<p id="aiConsent-error" className="mt-2 text-xs font-semibold text-[var(--error)]">{error("aiConsent")}</p>}</div><div className="flex flex-col-reverse gap-3 border-t border-[var(--border)] pt-6 sm:flex-row sm:items-center sm:justify-between"><p className="flex items-center gap-2 text-xs text-[var(--muted)]"><LockKeyhole className="size-4" aria-hidden="true"/>Saved as a private draft visible only to your SME.</p><Button type="submit" loading={pending}>Create private draft<ArrowRight className="size-4" aria-hidden="true"/></Button></div></form>}
