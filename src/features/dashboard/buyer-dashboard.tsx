import { PageHeading } from "@/components/page-heading";import { StatusBadge } from "@/components/status-badge";import { Card,CardContent } from "@/components/ui/card";import type { DashboardData } from "@/lib/demo/repository";import { Metrics,QueueButton } from "./shared";
export function BuyerDashboard({data}:{data:Extract<DashboardData,{role:"buyer"}>}){return <div className="page-enter mx-auto max-w-6xl"><PageHeading eyebrow="Buyer overview" title="Two suppliers are waiting." description="Confirm what your organization already knows so genuine suppliers can move forward faster."/><Metrics items={data.metrics}/><Card className="mt-8"><CardContent className="pt-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Pending confirmations</h2><p className="mt-1 text-sm text-[var(--muted)]">Oldest requests are shown first.</p></div><StatusBadge status="review">2 pending</StatusBadge></div><div className="mt-6 divide-y divide-[var(--border)]">{data.requests.map(r=><article key={r.id} className="grid gap-4 py-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><div className="flex flex-wrap gap-2"><b>{r.supplier}</b>{r.warnings>0&&<StatusBadge status="review">{r.warnings} warning</StatusBadge>}</div><p className="mt-2 text-sm text-[var(--muted)]">{r.invoice} · {r.amount} · Waiting {r.age}</p></div><QueueButton label="Review request"/></article>)}</div></CardContent></Card></div>}
