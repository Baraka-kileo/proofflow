import { PageHeading } from "@/components/page-heading";import { StatusBadge } from "@/components/status-badge";import { Card,CardContent } from "@/components/ui/card";import type { DashboardData } from "@/lib/demo/repository";import { Metrics,QueueButton } from "./shared";
export function FunderDashboard({data}:{data:Extract<DashboardData,{role:"funder"}>}){return <div className="page-enter mx-auto max-w-6xl"><PageHeading eyebrow="Funder overview" title="Evidence ready for review." description="Every package below includes deterministic checks and a buyer confirmation. Funding decisions remain yours."/><Metrics items={data.metrics}/><Card className="mt-8"><CardContent className="pt-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Buyer-confirmed applications</h2><p className="mt-1 text-sm text-[var(--muted)]">No unconfirmed or disputed packages appear here.</p></div><StatusBadge status="success">4 ready</StatusBadge></div><div className="mt-6 divide-y divide-[var(--border)]">{data.applications.map(a=><article key={a.id} className="grid gap-4 py-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><div className="flex flex-wrap items-center gap-2"><b>{a.supplier}</b><StatusBadge status={a.warnings?"review":"success"}>{a.warnings?`${a.warnings} review item`:"Checks clear"}</StatusBadge></div><p className="mt-2 text-sm text-[var(--muted)]">{a.buyer} · {a.amount} · Confirmed {a.confirmed}</p></div><QueueButton label="Review evidence"/></article>)}</div></CardContent></Card><p className="mt-4 text-xs text-[var(--muted)]">All offer and funding actions in ProofFlow are hackathon simulations.</p></div>}
